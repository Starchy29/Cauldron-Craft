using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// defines the method by which a move selects its choices
public class Selector
{
    // zone (square or radius)
    // one in range
    // aim in a direction
    // optional splash range (just visual)

    // tell the UI what to show and how to allow selection
    // tell the AI what options it has
    // tell the move the result
}
